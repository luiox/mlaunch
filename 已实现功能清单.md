# mlaunch 已实现功能清单

> 依据：`src/` 实际代码核对 + et 自动化走查验证（2026-08-31，feature/vb6-feature-gaps 分支）。
> VB6 Poner 对齐状态：核心交互全部落地，剩余长尾见 `未实现功能清单.md`。

## 一、窗口与视觉基线

- 无边框窗口，标题 `Poner`，独立阴影窗（`MLaunchShadow`，shadow_window.cpp）
- 顶栏 35px：标题 / 搜索 / 菜单 / 关闭（IconButtonUI + SVG 图标，icon_manager 解析）
- 搜索区灰底容器 + 白底输入区（对齐 VB6 UserControl 结构）
- 分组区 `0xFFE6E6E6`、列表区白底、边框 `0xFFD2D2D2`，flat 滚动条
- 全量中文文案（菜单/Toast/MessageBox/文件对话框/回收站显示名）

## 二、主界面结构

- 左分组列表 + 可拖拽分隔条（宽度持久化，钳制 80-600）+ 右项目列表
- 底部状态栏 + Toast 提示（StatusPresenter）
- 分组新增/重命名弹层（group_dialog，Enter 确认 / Esc 取消）

## 三、分组能力

- 新增 / 重命名 / 删除 / 清空分组（输入条目数 N 确认）
- 分组切换渲染、Ctrl+M 在分组上呼出分组菜单
- 回收站组（删除条目先进回收站，彻底删除有确认框）

## 四、项目 CRUD 与编辑窗

- 新增/编辑项目弹窗（MLaunchItemEditWindow）：名称/目标路径/参数/图标位置，路径浏览
- 删除（Del 键/菜单）→ 回收站；Ctrl+Z 撤销上次删除
- 单击选中、双击启动；拖拽文件落入窗口自动建条目（.lnk 自动解析目标）
- 启动计数、启动失败 toast 报错
- 分隔条条目（仅展示，不启动）

## 五、搜索模式（对齐 VB6）

- 放大镜按钮切入/切出（Esc 退出，含原生 EDIT 持焦时经 TranslateAccelerator 拦截）
- 关键字过滤（大小写不敏感，跨全部分组，显示 `[分组名]` 后缀）
- 命令语义：`cmd` / `setting` / `shutdown` / `reboot` / `logoff` / `screenoff` / `baidu <词>`（百度搜索）
- 过滤结果单击即启动；空输入显示三行提示
- **键盘选择（2026-08-31 新增）**：EDIT 持焦时上/下方向键移动结果选中项
  （TranslateAccelerator 拦截转发）、过滤/命令行自动选中首个有效行、回车启动选中项
  ——提示文案“上下键选择，回车键运行”承诺兑现

## 六、主菜单（Ctrl+M / Apps / 右键非列表区）

- **新建项目 ▸**：自定义项目… / 空项目 / 计算机 / 控制面板 / 回收站 / 注销 / 关机 / 重启
  - 系统条目用 `shell:::{CLSID}` 目标；注销/关机/重启用 `shutdown.exe /l|/s /t 0|/r /t 0`
- **按名称排序**（大小写不敏感稳定排序）/ **按使用频率排序**（启动次数降序稳定排序）
- **锁定**（见八）
- **自动隐藏**（见八）
- **路径转换 ▸**（见九）
- 导入数据 / 导出数据（JSON，导出为独立快照）
- 设置… / 退出

## 七、设置窗与全局热键

- 设置窗（420x366）：热键捕获（按键录入）、执行后隐藏、开机自启、分组栏宽度
- 全局热键（默认 Alt+1）切换主窗显隐；热键文本解析含 F1-F24/OEM 键
- 执行后隐藏（execute_hide）：启动条目成功后自动隐藏主窗
- 窗口尺寸/位置 ui_state.ini 持久化，恢复失败回退 settings 默认值

## 八、锁定与自动隐藏（本次新增）

- **锁定**：OnNcHitTest 将 HTCAPTION/HT* 边缘命中改判 HTCLIENT，拦截标题栏拖动与边缘缩放；
  列表条目拖拽准备与分隔条拖拽同步加锁守卫；`MF_CHECKED` 菜单勾选态；`settings.locked` 持久化
- **自动隐藏**：WM_ACTIVATE 失焦（WA_INACTIVE）且激活方属其他线程时 SW_HIDE；
  Alt+1 热键恢复；`settings.autoHide` 持久化
- et 走查验证：锁定时拖拽窗口不动、解锁后可拖；自动隐藏后激活他窗即隐、热键恢复

## 九、路径转换（本次新增）

- 子菜单：转为相对路径（便携模式）/ 转为绝对路径
- 占位符：`%pr%` = 程序目录（AppDir）、`%cr%` = 盘符根；target_path 与 icon_location 一并转换
- YESNO 确认框 → 批量转换 + toast 报告条数；journal `convert_paths to=relative|absolute count=N`；走 SaveData（自动备份先行）
- 转换规则含幂等（已是占位符跳过）、跨盘符跳过、精确等于程序目录的边界；24 个 core 单测覆盖往返

## 十、数据安全

- M0 安全层：原子写、损坏恢复、启动迁移器（P0-M 幂等）
- 备份：滚动 5 份 + 每日 30 份（backups/）
- journal（operations.log）：所有结构性操作可审计
- core 零 UI 依赖，tests/core_tests.cpp 26/26 通过

## 十一、其他

- 资源管理器菜单（近似）：SHParseDisplayName 成功则弹真实 shell 菜单，失败退回属性页
- 拖拽启动器内条目跨分组移动；journal 记录
- 日志 nassistant.log（INFO/WARN/DEBUG）

## 十二、2026-08-31 功能补全会话新增

- **单实例守卫**：CreateMutex 命中已有实例时还原并前置既有主窗后退出（第二实例
  exit 0，实测通过）；顺带消除第二实例 RegisterHotKey 失败的未定义行为
- **开机自启**：Settings.autorun + 设置窗复选框；HKCU Run 键写/删带引号 exe 路径，
  每次启动按设置同步（exe 挪位自愈、清理残留）；journal 记录
- **条目工作目录（起始位置）**：LaunchItem.working_dir 全链路——编辑窗“起始位置”行
  （含 IFileDialog 文件夹浏览）、JSON workingDir、Launch 传 ShellExecuteExW lpDirectory、
  %pr%/%cr% 占位符与环境变量展开、纳入路径转换；Poner 旧数据默认空天然兼容
- **条目禁用/启用**：条目菜单“禁用条目/启用条目”切换（幂等，journal set_item_enabled）；
  禁用项列表置灰（分组视图 + 搜索结果）、启动拦截报错
- **按使用频率排序**：主菜单新项（降序稳定排序，journal sort_group_by_count），
  让启动计数数据可感知
- **ui_state 首启落盘**：预创建目录/空文件 + flush 日志降级（err=2 为内核缓存误报，
  数据完整落盘已验证）
- **高 DPI 200% 定性**：不存在用户可见渲染 bug（详见未实现清单一节）；
  `tools/shot.ps1` 修复 PrintWindow 伪影（暗区检测回退屏幕实拍）；
  新增 `tools/scan_windows.ps1`（窗口枚举）与 `tools/uia_menu.ps1`（原生菜单 UIA 选择）
- 测试 24 → 26（autorun 持久化、禁用拦截、频率排序稳定性）
