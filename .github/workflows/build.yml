name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        mode: [release, debug]
    steps:
      - uses: actions/checkout@v4
        with:
          # 单层 submodule 即可（DuiLib + libca）；recursive 会递归进
          # DuiLib 的 SDL_ttf externals（无 .gitmodules 的历史死链）。
          submodules: true

      - uses: xmake-io/github-action-setup-xmake@3a1a5dddfc7fa625d9a698738334bf55655a861a
        with:
          xmake-version: latest

      - name: configure (${{ matrix.mode }})
        run: xmake f -p windows -a x64 -m ${{ matrix.mode }} -y

      - name: build
        run: xmake -y

      - name: test
        run: xmake run core_tests

      - name: locate exe
        id: exe
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Recurse -Filter "mlaunch.exe" | Select-Object -First 1
          "path=$($exe.FullName)" >> $env:GITHUB_OUTPUT

      - uses: actions/upload-artifact@v4
        with:
          name: mlaunch-windows-x64-${{ matrix.mode }}
          path: ${{ steps.exe.outputs.path }}
          if-no-files-found: error
