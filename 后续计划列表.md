# cpp-duilib-launcher 后续计划列表

## P-1：数据安全层（最高优先级，先于一切功能）

> 设计原则：可回滚的操作不设防，不可回滚的操作设大防。
> 安全性来自可回滚性，不来自输入咒语。

### 1. 备份体系（基于现有 WriteTextAtomic）

- [x] 每次保存前将旧 `launcher.v2.json` 轮转入 `backups/`，保留最近 5 份
- [x] 每日首次保存额外存日期快照 `launcher.v2.%Y%m%d.json`，保留 30 天
- [x] Load 时若检测到损坏，自动提示从最近备份恢复（Load 兜底已有，补"选备份"UI）

### 2. 删除分级防护

- [x] 单条删除 → 软删除：移入内置「回收站」分组 + Toast「已删除 · Ctrl+Z 撤销」，零摩擦
- [x] 回收站内删除 → 真删除，但可从任意备份找回（备份即最后防线）
- [x] 清空分组 / 批量删除 → 确认框要求输入**将被删除的条目数量**（强迫看清后果，比 MD5 可读）
- [x] 导入覆盖 / 重置全部 → 输入当前数据文件 MD5 前 6 位（核弹级仪式只留给核弹级操作）

### 3. 编辑流程两段式

- [x] 重命名/编辑统一走对话框：入口点击 → 对话框修改 → 明确「确定」提交
- [x] Esc 与失焦一律视为放弃，绝不静默落盘

### 4. 操作日志（追加型 journal）

- [x] 所有增删改移动追加一行到 `operations.log`（时间戳 + 动作 + 数据摘要）
- [ ] 用途：误操作可追溯；也为将来"时间机器"式回放留接口

---

## P0-M：Poner 数据迁移器（打通日常使用的最后一步）✅ 已完成

> 数据源实测：`D:\desktop\poner\Data.json`（44KB，12 组，字段与 v2 全同构）。
> 映射表：Name/TargetPath/IconLocation/Arguments/Count 一一直拷；
> 组名→Group.name+order；item_type/enabled/id 填默认。

- [x] `ImportPonerData(path)`：解析 → 按映射生成 LauncherData → 原子落盘
- [x] **幂等可重复导入**：以 (组名, TargetPath) 为键——已存在则更新 Count/名称，新条目追加
       （原因：迁移期 Poner 仍在日用，数据持续变化，需反复导入直到切换日）
       分隔条目按名称去重，同样幂等
- [x] 首次启动检测 `legacy_root\Data.json` 存在 → 弹「一键导入」提示
       （修复：DuiLib SetCurrentPath 会改写进程 CWD，legacy_root 改为启动时捕获）
- [x] 导入动作本身先走一遍安全层：导入前自动快照当前数据
- [x] 主菜单 Import Data 补全：文件选择器 + 导入（原占位项转正）

---

## P0：优先补齐用户最容易直接碰到的缺口

### 1. 搜索命令解析 ✅（2026-08-25 完成）

目标：让搜索框不仅能过滤，还能执行 VB6 风格命令。

实现（`SearchController::ParseCommand` + `ExecuteSearchCommand`）：

- [x] 在 `SearchController` 增加命令识别层
- [x] 支持精确命令：`cmd` / `setting` / `shutdown` / `reboot` / `logoff` / `screenoff`（后四者带确认框）
- [x] 支持“百度 + 关键词”模式
- [x] 保持原有名称过滤不回退

### 2. 项目编辑窗 MVP ✅（2026-08-25 完成）

目标：替代当前"重新选 exe"的简化编辑流程。

最小版本字段：

- [x] 标题
- [x] 路径
- [x] 参数
- [x] 图标位置

优先建议：

- [x] 先用简化 DuiLib 自绘窗完成 MVP
- [x] 先不追求完全复刻 VB6 布局，只保证字段完整

实现要点（独立弹出窗 `ItemEditWindow`，对齐 VB6 frmEdit）：

- 400x300 无边框 WS_POPUP，主窗居中；Esc 取消 / Enter 确认
- 任意空白处 HTCAPTION 拖拽（输入框/按钮/图标除外）
- 图标预览实时刷新（默认图标 / 修改图标 / 浏览）
- 编辑模式传 item_id，确认走 `UpsertItem` 更新而非新增（journal `update_item`）
- 已知坑：本 fork `WindowImplBase::MessageHandler` 是死代码，键盘必须走
  `HandleCustomMessage`；CEditUI 原生 EDIT 子窗口（类名 `EditWnd`）失焦即自毁、
  程序化打开时需手动补建并给焦点（见 `item_edit_window.cpp` 的
  `EnsureNativeEditFocused` 与 `kFocusEditMsg`）

### 3. 设置窗 MVP ✅（2026-08-25 完成）

目标：先把关键配置收口到一个统一入口。

实现（独立弹出窗 `SettingsWindow`，主菜单「设置」/ 搜索命令 `setting` 均可进入）：

- [x] 热键（`RegisterHotKey` 全局热键，Ctrl+M 式解析 `Alt+1`/`Ctrl+Alt+S`，留空禁用；WM_HOTKEY 切换主窗显隐）
- [x] 执行后隐藏（`execute_hide`，启动成功后按设置隐藏主窗）
- [x] 默认窗口宽高（无已保存布局时生效）
- [x] 分组栏宽度（确定后立即生效；无 ui_state 时也作默认值）
- 落盘走 `UpdateSettings`（钳制范围 + journal `update_settings` + 原子写 nassistant.settings.json）

### 4. 主菜单补全 ✅（2026-08-25 完成）

- [x] `Sort By Name`（按名称排序：core `SortGroupItemsByName`，大小写不敏感稳定排序 + journal）
- [x] `Import Data`（文件选择器 + 幂等导入）
- [x] `Export Data`（core `ExportData` 原子写出独立快照，另存为对话框）
- [x] `Settings`（打开设置窗）
- [x] VB6 的特殊“新建项目”子项（New Item → 自定义项目）
- [x] 附加：非列表区域右键 / Ctrl+M / Apps 键均可呼出主菜单

---

## P1：把界面风格进一步收敛到 VB6

### 1. 分组区背景改白 ✅（2026-08-25 确认）

- [x] 将 `group_panel` 从 `E6E6E6` 调整为 `FFFFFF`
- [x] 保留分隔线和 hover 的浅灰层次

### 2. 全量文案中文化 ✅（2026-08-25 完成）

- [x] 顶部菜单（新建项目/按名称排序/导入导出/设置/退出）
- [x] 分组右键菜单（新建/重命名/删除分组）
- [x] 项目右键菜单（管理员运行/所在位置/属性/复制路径/增删改/移动分组）
- [x] 状态栏提示（全部 Toast 中文化）
- [x] 对话框标题与按钮文字（含文件选择器过滤器、搜索命令行标签、回收站显示名）

### 3. 搜索区视觉继续收敛

- [x] 当前容器背景已对齐 `210`
- [ ] 下一步可微调输入框与边框，让它更接近 VB6 的灰底容器 + 白输入区感受

---

## P2：高还原度增强项

### 1. 独立阴影窗 / 辅助窗 ✅（2026-08-25 完成）

- [x] 复刻 `frmShadow` 的拖动与阴影行为
  （分层剪影窗：主窗 Z 序正下方 +7px 右下偏移、alpha 60 黑、点击穿透/不抢焦点/不进 Alt-Tab；
  WM_WINDOWPOSCHANGED 实时跟随，最小化/最大化/隐藏自动隐藏；DWM 软阴影关闭保持 VB6 硬边风格）

### 2. 完整资源管理器右键菜单 ✅（2026-08-25 完成）

- [x] 由“属性页近似实现”升级为真正的 Explorer context menu
  （`SHParseDisplayName` + `SHBindToParent` + `IContextMenu::QueryContextMenu/InvokeCommand`；
  非文件系统目标（URL 等）自动退回属性页）

### 3. 更细的像素级还原

- [x] 顶栏高度
- [x] 列表行高（34 → 28，对齐参考图）
- [x] 留白
- [x] hover/pressed 灰阶变化
- [x] 图标灰底框去除（26x26 灰底 → 20x20 透明，对齐 VB6 直绘图标）

---

## 推荐开发顺序

0. **数据安全层 P-1 全部**（备份/软删除/两段式/journal）✅
1. 搜索命令解析 ✅
2. 项目编辑窗 MVP ✅
3. 设置窗 MVP ✅
4. 主菜单补齐 ✅
5. 中文文案统一 ✅
6. 白底分组区与视觉收敛 ✅（搜索区输入框微调待做）
7. Explorer 真右键菜单 ✅
8. 阴影窗与像素级还原 ✅

> 计划内项目全部完成。可选打磨：搜索区输入框微调（更贴近 VB6 灰底容器 + 白输入区）。

> 优先级说明：可用性缺口（P-1/P0）优先于 VB6 还原度（P1/P2）。
> 还原度是情怀分，安全层和日用摩擦是生死分。

---

## 每阶段建议验证

### 功能验证

- `xmake build -r nassistant-duilib`
- 手工运行并验证菜单/搜索/编辑流程

### 回归验证

- `xmake run backend_tests`

### 文案与视觉验证

- 对照 `VB6_UI参数与需求分析.md`
- 对照 `C++实现_vs_VB6标准_逐项差异表.md`
- 逐项更新“已实现 / 未实现”文档
