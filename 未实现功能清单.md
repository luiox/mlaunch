# mlaunch 未实现功能清单

> 更新：2026-08-31（feature/vb6-feature-gaps 分支，第二批功能补全会话末）。
> 第二批（Explorer 菜单补全、滚轮调宽、设置页全量扩充）完成后，
> 代码核对口径下已无已知功能缺口；本文只留可选增强项。

## 一、可选增强（非缺口，均未排期）

- **高 DPI 清晰渲染**：当前 DPI-unaware + 系统拉伸（与 VB6 原版一致，无用户可见
  bug，见下方结案记录）。若追求 200% 下锐利渲染需做真正的 per-monitor DPI
  感知（fork Scale 管线 + 原生 EDIT 子窗口缩放），工程量大。
- **界面字体大小配置**：默认微软雅黑 14 全局字号，未提供设置项；
  需要把字号贯通到所有自绘控件与原生 EDIT，涉及面广。
- **搜索引擎切换**（google/bing）：搜索命令语义目前固定 `baidu <词>`。

## 二、已结案记录（保留备查）

### 高 DPI（200%）“黑帧”——截图工具伪影，非渲染 bug【2026-08-31 结案】

- **原结论作废**：屏幕实拍（CopyFromScreen）证实 200% 下窗口完整正常显示，
  内容铺满、无黑区，与 VB6 原版 Poner 行为一致（进程 DPI-unaware，系统拉伸显示，偏软但完整）。
- **伪影机制**：进程内一切度量均为逻辑坐标（实测 GetDpiForWindow=96、GetClientRect=482x645、
  fork SetDPI(96) 不缩放、非 layered 走 BitBlt 路径），物理窗口 964x1290 由系统虚拟化放大；
  而 PrintWindow 从高 DPI 感知进程调用时**不做 DWM 虚拟化拉伸**，把逻辑表面的原始内容
  1:1 放进物理尺寸位图——内容只占左上四分之一、其余填黑。此前走查截图全部经 shot.ps1
  (PrintWindow)，故“发现”黑帧。
- **修复**：`tools/shot.ps1` 增加暗区占比检测（>15% 即回退 CopyFromScreen 屏幕实拍），
  2026-08-31 实测回退触发、出图 0% 暗区。

### 第二批补全消化项【2026-08-31 完成】

- ~~资源管理器菜单仍是近似实现~~：占位符/环境变量展开 + IContextMenu2/3
  消息转发（WH_MSGFILTER）已补全。
- ~~锁定状态的长尾（分组栏宽度滚轮）~~：滚轮调宽已成为正式功能，
  锁定布局时禁用（TranslateAccelerator 层拦截）。
- ~~设置窗数值输入无实时反馈~~：越界边框变红、合法恢复，确定时钳制。
