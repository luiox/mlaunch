# mlaunch 未实现功能清单

> 更新：2026-08-31（feature/vb6-feature-gaps 分支）。本文只列“代码核对后仍缺失或仅部分实现”的项。
> 此前版本所列的主菜单补齐、搜索命令语义、编辑窗、设置窗、中文化、阴影窗等均已实现（见 `已实现功能清单.md`）。

## 一、高 DPI（200%）“黑帧”——已定性为截图工具伪影，非渲染 bug【2026-08-31 结案】

- **原结论作废**：屏幕实拍（CopyFromScreen）证实 200% 下窗口完整正常显示，
  内容铺满、无黑区，与 VB6 原版 Poner 行为一致（进程 DPI-unaware，系统拉伸显示，偏软但完整）。
- **伪影机制**：进程内一切度量均为逻辑坐标（实测 GetDpiForWindow=96、GetClientRect=482x645、
  fork SetDPI(96) 不缩放、非 layered 走 BitBlt 路径），物理窗口 964x1290 由系统虚拟化放大；
  而 PrintWindow 从高 DPI 感知进程调用时**不做 DWM 虚拟化拉伸**，把逻辑表面的原始内容
  1:1 放进物理尺寸位图——内容只占左上四分之一、其余填黑。此前走查截图全部经 shot.ps1
  (PrintWindow)，故“发现”黑帧。
- **修复**：`tools/shot.ps1` 增加暗区占比检测（>15% 即回退 CopyFromScreen 屏幕实拍），
  2026-08-31 实测回退触发、出图 0% 暗区。
- **遗留（可选增强，非 bug）**：若追求高 DPI 下清晰渲染，需做真正的 per-monitor DPI 感知
  （fork Scale 管线 + 原生 EDIT 子窗口缩放），工程量大，暂不排期。

## 二、资源管理器菜单仍是近似实现

`ShowSelectedItemShellMenu()`：SHParseDisplayName 成功则弹真实 shell 上下文菜单，
失败退回"打开属性"。与完整资源管理器右键菜单（含自定义动词/二级菜单）仍有差距。

## 三、锁定状态的长尾

- 已拦截：标题栏拖动、八向边缘缩放、列表条目拖拽、分隔条拖拽。
- 未拦截（VB6 原版行为待确认）：分组栏宽度的滚轮调整（若原版有此交互）。

## 四、设置窗细节

- 分组栏宽度输入框显示原始值，保存时才钳制到 80-600，输入过程中无实时反馈。
（开关类字段已全部使用自绘 `appui::CheckBoxUI`，含新增的开机自启复选框。）

## 五、杂项

- ~~`ui_state ini flush skipped err=2` 警告~~ 已修：预创建目录/空文件 + flush 日志降级
  Debug（首启实测可能仍有一条内核缓存误报，数据完整落盘已验证）。
- ~~路径转换未覆盖“工作目录”~~ 已修：`working_dir` 纳入转换范围（见已实现清单）。
