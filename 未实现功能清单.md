# mlaunch 未实现功能清单

> 更新：2026-08-31（feature/vb6-feature-gaps 分支，per-monitor DPI 会话末）。
> 第三批（per-monitor DPI 感知）完成后，代码核对口径下已无已知功能缺口；
> 本文只留可选增强项。

## 一、可选增强（非缺口，均未排期）

- ~~高 DPI 清晰渲染~~：**已实现 per-monitor v2 DPI 感知**（2026-08-31，见已实现
  清单·九），200% 下文字/图标原生渲染锐利，迁移对话框/菜单/阴影/图标全链路。
  跨显示器拖动跟随 WM_DPICHANGED。历史结案记录（黑帧伪影）保留于下节备查。
- **界面字体大小配置**：默认微软雅黑 14 全局字号，未提供设置项；
  需要把字号贯通到所有自绘控件与原生 EDIT，涉及面广。
- **搜索引擎切换**（google/bing）：搜索命令语义目前固定 `baidu <词>`。

## 二、已结案记录（保留备查）

### per-monitor DPI 感知落地方案要点【2026-08-31 完成】

- 进程级 PMv2（`SetProcessDpiAwarenessContext`，Win8.1 回退 `SetProcessDpiAwareness(2)`）。
- fork 的 PaintManager 默认 scale=96 且 `WindowImplBase::Create` 不会自动对齐，
  需在 WM_CREATE 最前调 `appui::AlignPaintManagerDpi`（root 未建时只设 scale 不重建资产，
  否则崩溃）；对话框用 `ScaleDialogToWindowDpi`（Create 后 SetDPI 放大 + 重居中）。
- ui_state.ini 统一存 96 基准逻辑值；恢复几何按**目标显示器** DPI（Shcore
  `GetDpiForMonitor`，须 LoadLibrary）换算；`GetFixedWidth`（物理）/`SetFixedWidth`
  （逻辑）读写不对称处一律 `ScaleIntBack` 反算（滚轮/拖拽/保存）。
- 陷阱备忘：fork `GetInterface` 按短名匹配（`DUI_CTR_BUTTON="Button"`，查
  "ButtonUI" 恒为空——曾让设置/编辑对话框按钮单击被"空白拖动"吞掉）；
  调试工具链多为 DPI-unaware（et 截图/坐标、PowerShell SetCursorPos 均为虚拟化
  坐标=物理÷缩放），跨进程验证坐标时必须先声明口径。

### 高 DPI（200%）“黑帧”——截图工具伪影，非渲染 bug【2026-08-31 结案】

- **原结论作废**：屏幕实拍（CopyFromScreen）证实 200% 下窗口完整正常显示，
  内容铺满、无黑区，与 VB6 原版 Poner 行为一致（进程 DPI-unaware，系统拉伸显示，偏软但完整）。
- **伪影机制**：进程内一切度量均为逻辑坐标（实测 GetDpiForWindow=96、GetClientRect=482x645、
  fork SetDPI(96) 不缩放、非 layered 走 BitBlt 路径），物理窗口 964x1290 由系统虚拟化放大；
  而 PrintWindow 从高 DPI 感知进程调用时**不做 DWM 虚拟化拉伸**，把逻辑表面的原始内容
  1:1 放进物理尺寸位图——内容只占左上四分之一、其余填黑。此前走查截图全部经 shot.ps1
  (PrintWindow)，故“发现”黑帧。
- **修复**：`tools/shot.ps1` 增加暗区占比检测（>15% 即回退 CopyFromScreen 屏幕实拍），
  2026-08-31 实测回退触发、出图 0% 暗区。

### 第二批补全消化项【2026-08-31 完成】

- ~~资源管理器菜单仍是近似实现~~：占位符/环境变量展开 + IContextMenu2/3
  消息转发（WH_MSGFILTER）已补全。
- ~~锁定状态的长尾（分组栏宽度滚轮）~~：滚轮调宽已成为正式功能，
  锁定布局时禁用（TranslateAccelerator 层拦截）。
- ~~设置窗数值输入无实时反馈~~：越界边框变红、合法恢复，确定时钳制。
