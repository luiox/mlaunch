# mlaunch 未实现功能清单

> 更新：2026-08-30（feature/vb6-feature-gaps 分支合并前）。本文只列“代码核对后仍缺失或仅部分实现”的项。
> 此前版本所列的主菜单补齐、搜索命令语义、编辑窗、设置窗、中文化、阴影窗等均已实现（见 `已实现功能清单.md`）。

## 一、高 DPI（200%）渲染异常【新发现，优先级高】

- 现象：系统缩放 200%（2880x1800 物理 / 1440x900 逻辑）下，窗口表面大面积黑帧，
  仅左上角有未缩放的内容痕迹；100% 缩放下完全正常。
- PrintWindow flag 0 与 2（PW_RENDERFULLCONTENT）均黑，说明是窗口自身表面内容异常，
  不是截图工具伪影；输入交互（菜单/journal/设置）在高 DPI 下仍正常。
- 待排查方向：fork GDI 后端的 DPI 缩放路径（`GetDPIObj()->ScaleRect` / 布局度量）、
  `UpdateLayeredWindow`/阴影窗在 WM_DPICHANGED 后的重绘；建议在 fork 仓库开 issue 复现。

## 二、资源管理器菜单仍是近似实现

`ShowSelectedItemShellMenu()`：SHParseDisplayName 成功则弹真实 shell 上下文菜单，
失败退回"打开属性"。与完整资源管理器右键菜单（含自定义动词/二级菜单）仍有差距。

## 三、搜索模式的键盘选择

- 搜索框持焦时上/下方向键不会移动结果列表选中项（原生 EDIT 吞掉方向键）；
  当前可用路径：鼠标单击结果直接启动。提示文案"上下键选择，回车键运行"与实际行为不完全一致。
- 可选改进：TranslateAccelerator 拦截方向键转发列表，或过滤后自动选中第一项。

## 四、锁定状态的长尾

- 已拦截：标题栏拖动、八向边缘缩放、列表条目拖拽、分隔条拖拽。
- 未拦截（VB6 原版行为待确认）：分组栏宽度的滚轮调整（若原版有此交互）。

## 五、设置窗细节

- "执行后隐藏"为按钮式开关（开/关换色），未用 CCheckBoxUI（其视觉态依赖图片资源）。
- 分组栏宽度输入框显示原始值，保存时才钳制到 80-600，输入过程中无实时反馈。

## 六、杂项

- `ui_state ini flush skipped err=2` 警告：关闭时 ui_state.ini 目录尚未创建导致一次落盘跳过（无功能影响）。
- 路径转换只覆盖 `target_path` 与 `icon_location`；若未来加"工作目录"字段需同步纳入。
- 高 DPI 下 ui_state.ini 记录的窗口物理尺寸与逻辑尺寸的换算未做归一化（跨缩放切换后窗口可能偏大/偏小）。
